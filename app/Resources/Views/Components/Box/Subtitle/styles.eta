<style>
    .box {
      margin: 5px;
      padding: 10px;
      border-radius: 5px;
    }
  
    .box-header {
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
  
    .box-content {
      overflow: hidden;
      max-height: 0;
      transition: max-height 0.3s ease-in-out;
    }
  
    .box.open .box-content {
      max-height: 100vh;
      transition: max-height 0.3s ease-in-out;
    }
  </style>